# 手柄按键映射

由于游戏原本支持全键盘操作，除摇杆等需要连续模拟量的操作外，普通按钮优先复用原版键盘语义，避免按 UI、对话和 3D 场景建立多套行为分支。

## 已验证

- A 键映射空格键。Win32 键盘路径负责 UI，PAL4 原生 InputManager 键码 `32` 负责 3D 交互；两者共享同一次按下/松开时序。

## 待验证

- B 键拟直接映射 Esc。当前仍保留系统菜单逐层返回与关闭逻辑，后续完成独立回归后再决定是否收敛。
